<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%=root_path%>">Aeropuerto</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if usuario_signed_in? %>
        <li class="active"><a href="<%= clientes_path %>">Clientes <span class="sr-only">(current)</span></a></li>
        <li><a href="<%=pasajes_path %>">Pasajes</a></li>
        <% end %>
        <li><a href="<%=aerolineas_path %>">aerolineas</a></li>
        <li><a href="<%=vuelos_path %>">Vuelos</a></li>
          <% if usuario_signed_in? %>
        <li><a href="<%=cliente_vuelos_path %>">Cliente Vuelos</a></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" >Informacion <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if usuario_signed_in? %>
            <li><a href="<%=itinerarios_path %>">Itinerario</a></li>
            <% end %>
            <li><a href="<%=origens_path %>">Origen</a></li>
            <li><a href="<%=destinos_path %>">Destino</a></li>

          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if usuario_signed_in? %>
            <li><a> <%= current_usuario.email %> </a></li>
              <li><a href="<%= edit_usuario_registration_path(current_usuario) %>">Editar Perfil</a></li>
              <li><a href="<%= destroy_usuario_session_path %>", data-method="delete">Cerrar Session</a></li>
            <% else %>
              <li><a href="<%= new_usuario_session_path %>">Ingresar</a></li>
              <li><a href="<%= new_usuario_registration_path %>">Registrarse</a></li>
            <% end %>
          </ul>

        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
</nav>
<style>
th{
padding: 20px;
}
</style>
<style>
body {background-color:darkseagreen}
h1   {color:yellow}
p    {color:gold}
li    {color:gold}
td    {color:gold}
</style>
</nav>
