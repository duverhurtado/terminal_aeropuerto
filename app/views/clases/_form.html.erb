<%= form_for(@clase) do |f| %>
  <% if @clase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clase.errors.count, "error") %> prohibited this clase from being saved:</h2>

      <ul>
      <% @clase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :t_clase %><br>
    <%= f.text_field :t_clase %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
